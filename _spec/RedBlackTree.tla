------------------------------- MODULE RedBlackTree ----------------------------- 
EXTENDS BinarySearchTree
VARIABLE rbnode
--------------------------------------------------------------------------------
RBNode == [node:Node, color:{"red", "black"}]
--------------------------------------------------------------------------------
TypeOK ==
  /\ rbnode \in RBNode
--------------------------------------------------------------------------------
RInsert(k) ==
  Insert(k)
--------------------------------------------------------------------------------
Constraint ==
(* w is father of v /\ v is red /\ v is not root tree*)
   \A v,w \in rbnode : ( /\ (w.left = v \/ w.right = v) /\ v.color = "red" /\ v = root) =>  w.color = "black"
================================================================================