------------------------------- MODULE RankTree ----------------------------- 
EXTENDS BinarySearchTree
--------------------------------------------------------------------------------
TypeOK ==
  /\ node \in [Key->[left:node, info:Val, right:node, rank:Nat]]
--------------------------------------------------------------------------------
RInsert(k) ==
  Insert(k)
--------------------------------------------------------------------------------
RankConstraint ==
   \A v,w \in node : ( /\ w.left = v \/ w.right = v) => (* w is father of v*)
   				  	  /\ v.rank \leq w.rank 
					  /\ w.rank \leq (v.rank + 1)
================================================================================